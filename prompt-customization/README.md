# prompt-customization Demo (TypeScript)

See the Python equivalent at `examples/prompt-customization/`.

## Quick Start
```bash
cd examples-ts/prompt-customization
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
