# tool-strategies Demo (TypeScript)

See the Python equivalent at `examples/tool-strategies/`.

## Quick Start
```bash
cd examples-ts/tool-strategies
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
