# recipes Demo (TypeScript)

See the Python equivalent at `examples/recipes/`.

## Quick Start
```bash
cd examples-ts/recipes
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
