# learning Demo (TypeScript)

See the Python equivalent at `examples/learning/`.

## Quick Start
```bash
cd examples-ts/learning
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
