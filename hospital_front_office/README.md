# hospital_front_office Demo (TypeScript)

See the Python equivalent at `examples/hospital_front_office/`.

## Quick Start
```bash
cd examples-ts/hospital_front_office
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
