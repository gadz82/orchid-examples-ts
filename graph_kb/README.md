# graph_kb Demo (TypeScript)

See the Python equivalent at `examples/graph_kb/`.

## Quick Start
```bash
cd examples-ts/graph_kb
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
