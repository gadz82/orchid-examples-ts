# PostgreSQL Storage Demo (TypeScript)

Demonstrates chat persistence with PostgreSQL using @orchid-ai/orchid-storage-postgres.

## Quick Start
```bash
cd examples-ts/postgres-storage
npm install && npm start
```

Requires a running PostgreSQL instance. Set `DATABASE_URL` env var.
