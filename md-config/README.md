# md-config Demo (TypeScript)

See the Python equivalent at `examples/md-config/`.

## Quick Start
```bash
cd examples-ts/md-config
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
