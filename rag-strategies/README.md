# rag-strategies Demo (TypeScript)

See the Python equivalent at `examples/rag-strategies/`.

## Quick Start
```bash
cd examples-ts/rag-strategies
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
