# travel-agency Demo (TypeScript)

See the Python equivalent at `examples/travel-agency/`.

## Quick Start
```bash
cd examples-ts/travel-agency
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
