# orchid_experts Demo (TypeScript)

See the Python equivalent at `examples/orchid_experts/`.

## Quick Start
```bash
cd examples-ts/orchid_experts
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
