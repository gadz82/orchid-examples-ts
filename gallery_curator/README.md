# gallery_curator Demo (TypeScript)

See the Python equivalent at `examples/gallery_curator/`.

## Quick Start
```bash
cd examples-ts/gallery_curator
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
