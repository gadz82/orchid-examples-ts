# car_dealer_fleet Demo (TypeScript)

See the Python equivalent at `examples/car_dealer_fleet/`.

## Quick Start
```bash
cd examples-ts/car_dealer_fleet
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
