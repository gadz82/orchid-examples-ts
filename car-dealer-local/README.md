# car-dealer-local Demo (TypeScript)

See the Python equivalent at `examples/car-dealer-local/`.

## Quick Start
```bash
cd examples-ts/car-dealer-local
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
