# architecture_review Demo (TypeScript)

See the Python equivalent at `examples/architecture_review/`.

## Quick Start
```bash
cd examples-ts/architecture_review
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
