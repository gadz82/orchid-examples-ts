# festival_producer Demo (TypeScript)

See the Python equivalent at `examples/festival_producer/`.

## Quick Start
```bash
cd examples-ts/festival_producer
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
