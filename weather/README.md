# weather Demo (TypeScript)

See the Python equivalent at `examples/weather/`.

## Quick Start
```bash
cd examples-ts/weather
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
