# mcp-auth Demo (TypeScript)

See the Python equivalent at `examples/mcp-auth/`.

## Quick Start
```bash
cd examples-ts/mcp-auth
npm install && npm start
```

Requires: Node.js 20+, Ollama with llama3.2 model.
